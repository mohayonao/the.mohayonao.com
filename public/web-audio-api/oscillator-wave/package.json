{
  "name": "oscillator-wave",
  "description": "show oscillator waves",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "babel-preset-es2015": "^6.1.2",
    "babelify": "^7.2.0",
    "browserify": "^12.0.1",
    "watchify": "^3.6.0"
  },
  "scripts": {
    "build": "browserify src -t babelify -o bundle.js",
    "watch": "watchify src -t babelify -o bundle.js --verbose"
  }
}
