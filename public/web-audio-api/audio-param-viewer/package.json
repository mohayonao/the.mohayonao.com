{
  "name": "audio-param-viewer",
  "description": "audio param viewer",
  "version": "1.0.0",
  "dependencies": {
    "object-assign": "^4.0.1"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.1.2",
    "babelify": "^7.2.0",
    "browserify": "^12.0.1",
    "watchify": "^3.6.0"
  },
  "private": true,
  "scripts": {
    "build": "browserify src -t babelify -o bundle.js",
    "watch": "watchify src -t babelify -o bundle.js --verbose"
  }
}
